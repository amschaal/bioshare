/urls.pyc
/wsgi.pyc
/media
settings/config.py
*.pyc
bioshareX/migrations/backup
.project
.pydevproject
.venv
.env.dev
*host_key*
.DS_Store

# Frontend revamp dev fixtures — local-only test harnesses for Vue primitives.
# Per frontend_revamp_plan.md Phase A: not collected into production static.
static_files/dev/
# STATIC_ROOT — build output of `manage.py collectstatic`, never edited by hand.
/static/
