/urls.pyc
/wsgi.pyc
/media
settings/config.py
*.pyc
bioshareX/migrations/backup
.project
.pydevproject
.venv
.env.dev
*host_key*
.DS_Store
.playwright-mcp
# Frontend revamp dev fixtures — local-only test harnesses for Vue primitives.
# Per frontend_revamp_plan.md Phase A: not collected into production static.
static_files/dev/
# STATIC_ROOT — build output of `manage.py collectstatic`, never edited by hand.
/static/

# Playwright session cache for the docs screenshot scripts. Contains a live
# session cookie -- never commit.
tools/screenshots/auth.json
tools/screenshots/node_modules/

# mkdocs build output (see docs/requirements.txt, .github/workflows/docs.yml).
/site/
